const reverseArray = <T>(arr: T[]): T[] => {
  return arr.reverse();
};

// const arr = [1, 2, 3, 4, 5];
// reverseArray(arr);
// console.log(arr);

// const arr = [1, 2, 3];
// const reversedArr = reverseArray(arr);
// console.log(reversedArr === arr);
